# spelunk into a directory

spelunk recursively goes through the current directory and counts
how many files of different types (by extension) exist and prints
a summary.

## Usage

```
# Compile it
$ cc -o spelunk spelunk.c

# Run it in some directory
$ ~/path/to/binary/spelunk

#... enjoy the output...

```
